// Position - X, Y Coordinates within the maze grid
export class Pos {
    public row: number;
    public col: number;

    constructor(row: number, col: number) {
        this.row = row;
        this.col = col;
    }
}
