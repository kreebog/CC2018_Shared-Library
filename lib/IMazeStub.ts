/**
 * Interface for Maze Stub Data - Used when working with lists of mazes.
 */
export interface IMazeStub {
    id: string;
    height: number;
    width: number;
    seed: string;
    url: string;
}
